package com.oficinaapp.oficina_app.domain.enums;

/**
 * Visual identity of a service category. The API only carries the key; icon
 * and colour are decided by the app.
 */
public enum ServiceCategoryStyle {
    ENGINE,
    BRAKES,
    SUSPENSION,
    ELECTRICAL,
    FLUIDS,
    GENERIC
}
