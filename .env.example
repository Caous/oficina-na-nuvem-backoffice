# Copy this file to .env and pick your own values. The .env file is
# gitignored and must never be committed.
DB_NAME=oficina_db
DB_USER=oficina
DB_PASSWORD=change-me

# Host ports. Change them if something else already listens there — the
# API and Docker read the same values.
DB_PORT=5432
SERVER_PORT=8080
